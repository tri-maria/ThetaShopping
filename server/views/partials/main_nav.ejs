<!--Main Navigation Section-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="\home"><img src="Assets/images/ThetaLogo.jpg" height="20%" width="20%"/><b style="margin-left: 20px;">THETA Shopping Mall</b></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto mb-2 mb-lg-0"  style="margin-right: 200px;">
          <% if (title == "Men") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>
            <a class="nav-link " aria-current="page"  href="\men">Men</a>
          </li>
          <% if (title == "Women") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>
            <a class="nav-link " aria-current="page" href="\women">Women</a>
          </li>
          <% if (title == "Kids") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>
            <a class="nav-link " aria-current="page" href="\kids">Kids</a>
          </li>
          <% if (title == "Offers") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>
            <a class="nav-link " aria-current="page" href="\offers">Offers</a>
          </li>
        </ul>
        <ul class="navbar-nav ml-auto mb-2 mb-lg-0" style="margin-left: 200px;">

        <% if((typeof displayName == 'string' && displayName) || session.cart) { %>
            
          <li class="nav-item">
            <a class="nav-link " aria-current="page" href="\profile"><i class="fas fa-lg fa-user"></i> Profile</a>
          </li>
          
          <li class="nav-item">
            <a class="nav-link " href="\cart" ><i class="fas fa-lg fa-shopping-cart"></i>
              Cart(<span id="cart-counter"><%= session.cart ? session.cart.totalQty : '' %></span>)</a>
          </li>
          
          <li class="nav-item">
              <a class="nav-link" href="/logout"><i class="fas fa-lg fa-sign-out-alt"></i> Logout</a>
          </li>  

          <% } else { %>
            <li class="nav-item">
              <a class="nav-link " aria-current="page" href="\login"><i class="fas fa-lg fa-user"></i> Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link " id="cart-counter"  href="\login"><i class="fas fa-lg fa-shopping-cart"></i>
                Cart(<span id="cart-counter"><%= session.cart ? session.cart.totalQty : '' %></span>)</a>
              </li>
              <li class="nav-item">
                <a class="nav-link " aria-current="page" href="\login"><i class="fas fa-lg fa-sign-in-alt"></i> Login</a>
              </li>
          <% } %>
      </ul>
    </div>
  </nav>